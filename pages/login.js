import Layout from "../components/Layout";
import fetch from "node-fetch";

function Login({ tasks }) {
    return (
        <Layout>
            <h1>Login</h1>
        </Layout>
    )
}

export default Login;